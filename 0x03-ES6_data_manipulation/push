#!/bin/bash

# Perform 'git add .' to stage all changes
git add .

# Ask the user for the commit message
read -p "Enter your commit message: " commit_message

# Perform 'git commit' with the user input as the commit message
git commit -m "$commit_message"

# Perform 'git push' to push the changes to the remote repository
git push
